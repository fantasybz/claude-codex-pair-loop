#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"

cd "$ROOT_DIR"
echo "[post-commit] Starting local live Claude/Codex E2E in the background"
bash scripts/local-ci.sh live-background
